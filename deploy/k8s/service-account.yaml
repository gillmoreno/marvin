# The Pod's identity. Add cloud IAM annotations here (e.g. EKS IRSA, GKE Workload Identity) if the agent needs
# cloud credentials without a static key.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: marvin-sa
  labels:
    app: marvin
